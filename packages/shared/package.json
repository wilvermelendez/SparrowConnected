{
  "name": "sparrow-connected-shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "restoreWorkspaces": "rm -rf node_modules && rm -f package-lock.json"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "UNLICENSED"
}
